spring:
  profiles:
     active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/repo/shopping-app/config

server:
  port: 8888

security:
  user:
    name: user
    password: foo

